# Archived Data

This folder stores inactive or superseded files that are not part of the live app dataset.

- `legacy_snapshots/` keeps archived CSVs that were validated as unused by the current app.
- Archive files here before deleting anything.
- The live app should continue to read only from `data/`.
